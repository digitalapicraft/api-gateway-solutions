### Case: secret-as-key — the app's SECRET sent where its KEY belongs.
### Expect 401. secret_validation is off in this package, so the secret is not an
### accepted alternative credential. Turning it on would make this request succeed.
GET {{GATEWAY}}/fleet/price-list
X-Device-Key: {{APP_SECRET}}
accept: application/json
