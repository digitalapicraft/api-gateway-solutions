### Case: single-masked — one record, through the regex path rewrite.
### Expect 200 and the same masking. A 404 here is a routing problem, not a
### masking one: the id is carried to the upstream by proxy-rewrite's regex_uri.
GET {{GATEWAY}}/support/customers/3
accept: application/json
