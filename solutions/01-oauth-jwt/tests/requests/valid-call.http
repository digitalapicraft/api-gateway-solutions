### Case: valid-token — a token issued by POST /oauth/token, on a protected route.
### Expect 200.
### A 401 here with a freshly issued token means the signing secret differs
### between the issue route and the validate route.
GET {{GATEWAY}}/orders
authorization: Bearer {{ACCESS_TOKEN}}
accept: application/json
