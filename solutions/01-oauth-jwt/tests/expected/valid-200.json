{
  "_case": "valid-token",
  "_status": 200,
  "_note": "The upstream's own response body, passed through untouched — this solution adds authentication, not transformation. Assert the status and that the body is your backend's normal payload. The array below is illustrative for an Orders API.",
  "_headers_present": ["X-Request-Id"],
  "_body_example": [
    { "orderId": "ord_1a2b3c", "status": "open", "customerRef": "cust_9f8e" }
  ]
}
